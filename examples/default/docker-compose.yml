services:
  gardarr:
    image: ghcr.io/jfxdev/gardarr:latest
    container_name: gardarr
    # Run as host user to ensure proper filesystem permissions
    # Use: user: "1000:1000" or user: "${UID}:${GID}" in .env file
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "${APP_PORT:-3200}:3200"
    environment:
      APP_PORT: ${APP_PORT:-3200}
      # APP_URL should be origin-only: http://example.com or http://localhost:3200 (no trailing slash or path)
      APP_URL: ${APP_URL:-http://localhost:3200}
      # Required: a Base64-encoded 32-byte key. Generate with: openssl rand -base64 32
      ENCRYPTION_KEY: "${ENCRYPTION_KEY:?Set ENCRYPTION_KEY in .env}"
    volumes:
      # Use /data to persist SQLite database
      - gardarr_data:/data
      # Use /media to persist images and media files
      - gardarr_media:/media
    restart: unless-stopped
    networks:
      - gardarr-network
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3200/v1/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s

volumes:
  gardarr_data:
    driver: local
  gardarr_media:
    driver: local

networks:
  gardarr-network:
    driver: bridge
